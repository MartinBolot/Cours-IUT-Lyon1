const partReducer = (state={}, action) => {
  /*switch (action.type) {
    case "UPDATE_SELECTED_ROBOT":
      return action.obj;
      break;
    default: {
      return action.state;
    }
  }*/
  return state;
};

export default partReducer;
